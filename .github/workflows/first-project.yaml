name: First Project Test Action
on: 
  pull_request:
    paths:
      - 'first-project/**'
jobs:
  only-build-first:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Start of job on first project"
      - run: echo "End of job on first project"